# v0.1.0 Verification

- Home Assistant 2026.7.2 Docker smoke test: PASS (`dev/compose.yaml`, container healthy)
- Mock API suite: PASS (`pytest -q`, 268 passed; `ruff check .`; `ruff format --check .`; `mypy custom_components/toss_invest`)
- Real API read-only setup: PASS — config entry `BROKERAGE ••••3060` created via Config Flow with real Toss OAuth credentials entered directly in the browser (never pasted into a terminal, fixture, or this document)
- KR/US entity checks: PASS — 3 devices, 93 entities: `스페이스X` (US, 24 entities), `TIGER 미국우주테크` (KR ETF, 24 entities), `Toss Invest Portfolio` (aggregate, 45 entities incl. KOSPI/KOSDAQ indicators, KR/US allocation, KRW/USD exchange rate)
- Coordinator refresh checks: PASS — dev container logs show holdings, prices, candles, warnings, buying power, rankings, reference, and market-context coordinators all completing with `success: True`; open-price coordinator observed re-running on its schedule
- Privacy default-on check: PASS — `switch.toss_invest_portfolio_privacy_mode` state is `on` by default
- Manual refresh control: PASS — `button.toss_invest_portfolio_refresh` entity present and registered
- Alert event entity: PASS — `event.toss_invest_portfolio_alert` present and registered
- Light/dark and desktop/mobile dashboard checks: NOT PERFORMED this pass — dev container has no dashboard wired up; covered instead by the automated `tests/test_yaml_artifacts.py` YAML/theme-safety suite (PASS)
- Rate-limit and sanitized-log checks: PASS — `docker logs toss_invest_dev_ha` scanned for `client_secret`/`access_token`/`authorization`/`bearer`: zero matches
- Order mutation/history/conditional source scan: PASS

Ran: `rg -n "client_secret|access_token|X-Tossinvest-Account|/orders|/conditional-orders" . --glob '!docs/superpowers/**' --glob '!tests/**'`
Result: only intentional constant/schema/field-name references (config flow fields, translations, header name, auth payload key); no logged values, no order or conditional-order endpoint calls.

## Notes

- Account identifier is masked in the UI (`••••3060`); no `client_id`/`client_secret`/`access_token` value was ever pasted into a terminal, screenshot, or this document.
- This verification does not constitute production approval. Per the implementation plan, production installation requires the user to review this record and explicitly approve deployment via the release/HACS path (not a direct file copy).
